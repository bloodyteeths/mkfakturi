# Minimal .dockerignore - only exclude what's absolutely necessary

# Exclude node_modules (will be installed during build)
node_modules

# Exclude vendor (will be installed during build)
vendor

# Exclude .git (don't need version control in image)
.git

# Exclude development files
.vscode
.idea
*.swp
.DS_Store

# Exclude logs
*.log
logs/
storage/logs/*

# Exclude test output
tests/_output
cypress/screenshots
cypress/videos
.phpunit.result.cache

# Exclude temporary storage
storage/framework/cache/*
storage/framework/sessions/*
storage/framework/views/*

# Exclude docker compose files (not needed in image)
docker-compose*.yml

# Exclude docs
docs/

# Exclude environment files (will be created at runtime)
.env
.env.*
!.env.example

# Exclude build artifacts (will be built during Docker build)
public/build
public/storage

# Everything else is INCLUDED by default
