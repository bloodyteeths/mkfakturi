# .dockerignore - Exclude what's not needed, explicitly include build inputs

# === Exclude directories that will be rebuilt ===
node_modules
vendor
facturino/
mcp-server/
ai-service/
cypress/
playwright-report/
tests/
docs/
roadmaps/
samples/
screenshots/

# === Exclude VCS ===
.git

# === Exclude development files ===
.vscode
.idea
*.swp
.DS_Store

# === Exclude logs ===
*.log
logs/

# === Exclude test output ===
tests/_output
cypress/screenshots
cypress/videos
.phpunit.result.cache

# === Exclude docker compose ===
docker-compose*.yml

# === Exclude environment files ===
.env
.env.*
!.env.example

# === Explicitly INCLUDE required build files ===
# These negations ensure files are in build context even if Docker caches are stale
!composer.json
!composer.lock
!package.json
!package-lock.json
!vite.config.js
!postcss.config.cjs
!tailwind.config.js

# === Explicitly INCLUDE source directories ===
!resources/
!lang/
!public/

# === But exclude these public subdirectories ===
public/build
public/storage

# === Exclude storage subdirectories ===
storage/logs/*
storage/framework/cache/*
storage/framework/sessions/*
storage/framework/views/*
